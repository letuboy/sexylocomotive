- title t('.title')
%p!= t('.help')

= semantic_form_for @category, :url => admin_categories_url do |form|
  = render 'form', :f => form
  = render 'admin/shared/form_actions', :back_url => admin_photo_galleries_url, :button_label => :create
